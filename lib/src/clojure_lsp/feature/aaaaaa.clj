(ns clojure-lsp.feature.aaaaaa
  (:require
   [clojure.string :as str]))


(str/blank? "")


(def x [<list:
         (ns clojure-lsp.feature.aaaaaa
           (:require
            [clojure.string :as str]))
       > {:l [], :pnodes [<forms:
         (ns clojure-lsp.feature.aaaaaa
           (:require
            [clojure.string :as str]))


         (str/blank? "")
       >], :ppath nil, :r (<newline: "\n\n\n"> <list: (str/blank? "")>)}])